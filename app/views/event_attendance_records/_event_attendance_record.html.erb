<div class="table table-striped table-bordered">
    <table>
      <tr>
        <td><%= event_attendance_record.student.fname %></td>
        <td><%= event_attendance_record.student.lname %></td>
        <td><%= event_attendance_record.student.my_allergies %></td>
        <td><%= event_attendance_record.student.my_teacher %></td>
        <td><%= event_attendance_record.present %>
        <%= link_to "present toggle", present_toggle_event_attendance_record_path(event_attendance_record), method: :patch %>
        </td>
        <td><%= event_attendance_record.brought_lunch %> <%= link_to "brought_lunch toggle", brought_lunch_toggle_event_attendance_record_path(event_attendance_record), method: :patch %></td>
        <td><%= event_attendance_record.fed %> <%= link_to "fed toggle", fed_toggle_event_attendance_record_path(event_attendance_record), method: :patch %></td> 
        <td><%= event_attendance_record.milk_only %> <%= link_to "milk only toggle", milk_only_toggle_event_attendance_record_path(event_attendance_record), method: :patch %></td>
        <td><%= link_to 'Edit', edit_event_attendance_record_path(event_attendance_record) %></td>
      </tr>
    </table>
</div>